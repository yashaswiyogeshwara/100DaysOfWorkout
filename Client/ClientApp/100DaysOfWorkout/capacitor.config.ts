import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '100DaysOfWorkout',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
